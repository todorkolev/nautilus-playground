---
# Indicator Comparison Strategy Configuration

strategy:
  module: src.strategies.indicator_comparison.strategy
  class: IndicatorComparisonStrategy
  config_class: IndicatorComparisonConfig

parameters:
  instrument_id: BTCUSDT.BINANCE
  bar_type: BTCUSDT.BINANCE-1-HOUR-LAST-EXTERNAL
  rsi_period: 14
  rsi_oversold: 30
  rsi_overbought: 70
  fast_ma_period: 10
  slow_ma_period: 20
  trade_size: 0.010000

data:
  instruments:
    - id: BTCUSDT.BINANCE
      bar_types:
        - 1-HOUR-LAST
